import get from "./get";

export default {
  /**
   * Возвращает расширенную информацию о пользователях
   ** Поля counters, military будут возвращены только в случае,
   *  если передан ровно один user_id
   */
  get
};
